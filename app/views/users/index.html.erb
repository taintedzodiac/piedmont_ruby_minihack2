<h1>Users</h1>
<%- @users.each do |user| %>
  <p>
    <%= link_to user.username, user %>
    <%- user.tweets.each do |tweet| %>
      <p><%= render partial: 'tweets/tweet', locals: { tweet: tweet } %></p>
    <%- end %>
  </p>

<%- end %>
